
let initialState ={
    friendsData: [
        { id: 1, name: "Mars" },
        { id: 1, name: "Mercury" },
        { id: 1, name: "Jupiter" },
      ],
}

const friendsReducer = (state = initialState, action) =>{
    
    return state;
};

export default friendsReducer;
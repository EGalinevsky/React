
let initialState ={
    friendsData: [
        { id: 1, name: "Mars" },
        { id: 2, name: "Mercury" },
        { id: 3, name: "Jupiter" },
      ],
}

const friendsReducer = (state = initialState, action) =>{
    
    return state;
};

export default friendsReducer;